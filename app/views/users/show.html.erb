<div class="container">
  <h2>詳細画面</h2>
  <h3>Following Users</h3>
  　<% @user.followed_users.each do |user| %>
  <%= profile_img(user) %>
  <%= user.name %>
    <% end %>
    <br>
    <br>
  <h3>Followers</h3>
    <% @user.followers.each do |user| %>
    <%= profile_img(user) %>
    <%= user.name %>
      <br>
    <% end %>
    <br>
</div>
